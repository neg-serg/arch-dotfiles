#!/bin/dash
export ENV=$HOME/.profile
xrdb -I$XDG_CONFIG_HOME/xres -load "$HOME/.Xresources"
if [ -d /etc/X11/xinit/xinitrc.d ] ; then
    for f in /etc/X11/xinit/xinitrc.d/?*.sh ; do
        [ -x "$f" ] && . "$f"
    done
    unset f
fi
exec $XDG_CONFIG_HOME/i3/bin/i3-gnome
