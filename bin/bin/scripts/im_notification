#!/bin/zsh
appname="$1"
summary="$2"
body="$3"
image="$4"
urgency="normal"

main(){
    body="$(par 70 <<< $body)"
    dunstify -p -a "instant-message" -u "${urgency}" -i "${image}" "${summary}" "${body}"
}

main "$@"
