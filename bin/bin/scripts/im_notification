#!/bin/zsh
appname="$1"
summary="$2"
body="$3"
icon="/home/neg/pic/meow/07saq_QzaP4.jpg"
urgency="normal"

main(){
    body="$(par 60 <<< $body)"
    dunstify -p -a "instant-message" -u "${urgency}" -i "${image}" "${summary}" "${body}"
}

main "$@"
