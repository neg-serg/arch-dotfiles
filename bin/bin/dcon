#!/bin/dash

main() {
    CONTAINER=$(docker ps --format 'table {{.ID}}\t{{.Status}}\t{{.Command}}' \
        | tail -n +2 \
        | grep -v '/portainer' \
        | fzf \
        | sed 's/\ .*//g')
    docker exec -i -t "${CONTAINER}" bash
}
