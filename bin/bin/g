#!/usr/bin/env zsh
export GOOGLE_ARGS='["--count", 5]'
export ROFI_SEARCH='googler'
rofi -modi blocks \
     -blocks-wrap /usr/bin/rofi-search \
     -show blocks -lines 4 -eh 4 -kb-custom-1 'Control+y' \
     -theme $XDG_CONFIG_HOME/rofi/neg.rasi
