[Unit]
Description=Compositing manager
After=dbus.service

[Service]
Type=simple
ExecStartPre=/bin/sleep 2
ExecStart=/usr/bin/picom --backend xrender --dbus --experimental-backends --backend glx

[Install]
WantedBy=default.target
