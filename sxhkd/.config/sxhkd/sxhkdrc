super + apostrophe
    i3-msg restart
XF86AudioMute
    pactl set-sink-mute 1 toggle
super + quotedbl
    i3-msg reload
XF86Audio{Prev,Next,Play,Stop}
    mpc -q {prev,next,toggle,stop}
super + control + XF86Audio{Raise,Lower}Volume
    pactl set-sink-volume @DEFAULT_SINK@ {+,-}5%
super + 8
    mpc -q volume 0 || amixer -q set Master 0% mute
super + shift + 8
    mpc -q volume 100 || amixer -q set Master 100% unmute
{super + control + shift + u,XF86Sleep}
    sudo systemctl suspend
super + U
    eject -T
super + c
    ~/bin/clip
super + u
    udiskie-umount -a
super + M
    ~/bin/scripts/rofi_mpd
super + control + r ; { c, d, i, r }
    { \
        xcalib -c, \
        xcalib -alter -invert, \
        xcalib -alter -invert, \
        xcalib -gc 1.08 -co 97 ~/.config/color_profiles/x230.icm
    }
alt + grave
    rofi -no-plugins -sort -show run -lines 1 -columns 4 -case-sensitive=false -matching fuzzy -theme-str '* \{ font: "Iosevka Term Medium 15"; \}' -theme-str '#window \{ width:1900; y-offset: -32; location: south; anchor: south; \}' -disable-history
super + shift + {6,7}
    { ~/bin/wl, ~/bin/wl -r }
# vim: set ft=sxhkdrc:
