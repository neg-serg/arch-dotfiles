super + apostrophe
    i3-msg restart
XF86AudioMute
    pactl set-sink-mute 1 toggle
super + quotedbl
    i3-msg reload
XF86Audio{Prev,Next,Play,Stop}
    playerctl {previous,next,play-pause,stop}
super + control + XF86Audio{Raise,Lower}Volume
    pactl set-sink-volume @DEFAULT_SINK@ {+,-}5%
super + 8
    playerctl volume 0.0 || amixer -q set Master 0% mute
super + shift + 8
    playerctl volume 1.0 || amixer -q set Master 100% unmute
{super + control + shift + u,XF86Sleep}
    sudo systemctl suspend
super + U
    eject -T
super + c
    ~/bin/clip
super + u
    udiskie-umount -a
super + M
    ~/bin/scripts/rofi_mpd
super + control + r ; { c, d, i, r }
    { \
        xcalib -c, \
        xcalib -alter -invert, \
        xcalib -alter -invert, \
        xcalib -gc 1.08 -co 97 ~/.config/color_profiles/x230.icm
    }
alt + grave
    rofi -no-plugins -sort -show run -lines 1 -columns 4 -case-sensitive=false -matching fuzzy -theme-str '* \{ font: "Iosevka Term Medium 20"; \}' -theme-str '#window \{ width:1900; y-offset: -32; location: south; anchor: south; \}' -disable-history
super + shift + {5,6}
    { \
        ~/bin/wl -s, \
        ~/bin/wl -r, \
        ~/bin/wl -v
    }
# vim: set ft=sxhkdrc:
