AURDEST  DEFAULT=${HOME}/src/1st_level/pacaur"
BROWSER  DEFAULT="firefox"
CARGO_HOME  DEFAULT="${XDG_DATA_HOME}"/cargo
CCACHE_CONFIGPATH  DEFAULT="${XDG_CONFIG_HOME}"/ccache.config
CCACHE_DIR  DEFAULT="${XDG_CACHE_HOME}"/ccache
CUDA_CACHE_PATH  DEFAULT="${XDG_CACHE_HOME}"/nv
DISPLAY  DEFAULT=${REMOTEHOST}:0.0 OVERRIDE=${DISPLAY}
GHCUP_USE_XDG_DIRS  DEFAULT=true
GNUPGHOME  DEFAULT="${XDG_DATA_HOME}"/gnupg
GNUPGHOME  DEFAULT="${XDG_DATA_HOME}"/gnupg
GOPATH  DEFAULT=/opt/go
GREP_COLOR  DEFAULT='37;45'
GREP_COLORS  DEFAULT='ms=0;32:mc=1;33:sl=:cx=:fn=1;32:ln=1;36:bn=36:se=1;30'
HTTPIE_CONFIG_DIR  DEFAULT="${XDG_CONFIG_HOME}"/httpie
_JAVA_OPTIONS  DEFAULT=-Djava.util.prefs.userRoot  DEFAULT="${XDG_CONFIG_HOME}"/java
LIBSEAT_BACKEND  DEFAULT=logind
MPV_HOME  DEFAULT="${XDG_CONFIG_HOME}/mpv"
NB_DIR  DEFAULT="${XDG_DATA_HOME}/nb"
NBRC_PATH  DEFAULT="${XDG_CONFIG_HOME}/nbrc"
NMBGIT  DEFAULT="${XDG_DATA_HOME}"/notmuch/nmbug
NOTMUCH_CONFIG  DEFAULT="${XDG_CONFIG_HOME}"/notmuch/notmuchrc
PARALLEL_HOME  DEFAULT="${XDG_CONFIG_HOME}"/parallel
PASSWORD_STORE_DIR  DEFAULT="${XDG_DATA_HOME}"/pass
PYLINTHOME  DEFAULT="${XDG_CONFIG_HOME}"/pylint
QT_QPA_PLATFORMTHEME  DEFAULT="qt5ct"
RUSTC_WRAPPER  DEFAULT=sccache
RUSTUP_HOME  DEFAULT="${XDG_DATA_HOME}"/rustup
SSH_AUTH_SOCK  DEFAULT="${XDG_RUNTIME_DIR}/keyring/ssh"
TERMINFO  DEFAULT="${XDG_DATA_HOME}"/terminfo
TERMINFO_DIRS  DEFAULT="${XDG_DATA_HOME}"/terminfo:/usr/share/terminfo
VAGRANT_HOME  DEFAULT="${XDG_DATA_HOME}"/vagrant
WINEPREFIX  DEFAULT="${XDG_DATA_HOME}"/wineprefixes/default
XAUTHORITY  DEFAULT="${XDG_RUNTIME_DIR}"/Xauthority
XDG_CACHE_HOME  DEFAULT=${HOME}/.cache
XDG_CONFIG_HOME  DEFAULT=${HOME}/.config
XDG_DATA_HOME  DEFAULT=${HOME}/.local/share
XDG_DESKTOP_DIR  DEFAULT=${HOME}/.local/desktop
XDG_DOCUMENTS_DIR  DEFAULT=${HOME}/doc/
XDG_DOWNLOAD_DIR  DEFAULT=${HOME}/dw
XDG_MUSIC_DIR  DEFAULT=${HOME}/music
XDG_PICTURES_DIR  DEFAULT=${HOME}/pic
XDG_PUBLICSHARE_DIR  DEFAULT=${HOME}/1st_level/upload/share
XDG_STATE_HOME  DEFAULT=${HOME}/.local/state
XDG_TEMPLATES_DIR  DEFAULT=${HOME}/1st_level/templates
XDG_VIDEOS_DIR  DEFAULT=${HOME}/vid
XINITRC  DEFAULT="${XDG_CONFIG_HOME}"/xinit/xinitrc
XSERVERRC  DEFAULT="${XDG_CONFIG_HOME}"/xinit/xserverrc
ZDOTDIR  DEFAULT="${XDG_CONFIG_HOME}/zsh"
